"  // Effect to recalculate when pins or areas change (e.g. toggle, delete, load)\n  useEffect(() => {\n    calculateInteractions(activePins, activeAreas);\n  }, [activePins, activeAreas, calculateInteractions]);\n\n  // Supabase Realtime Session & Presence Tracker\n  useEffect(() => {\n    if (!isSessionActive || !projectId) {\n      if (channelRef.current) {\n        channelRef.current.unsubscribe();\n        channelRef.current = null;\n      }\n      setSessionListeners([]);\n      return;\n    }\n\n    const channelName = `session:${projectId}`;\n    const channel = supabase.channel(channelName);\n    channelRef.current = channel;\n\n    channel\n      .on('presence', { event: 'sync' }, () => {\n        const presenceState = channel.presenceState();\n        const listeners: any[] = [];\n        Object.values(presenceState).forEach((presences: any) => {\n          presences.forEach((p: any) => {\n            if (p.listenerId && p.name) {\n              if (!listeners.some(l => l.listenerId === p.listenerId)) {\n                listeners.push(p);\n              }\n            }\n          });\n        });\n        setSessionListeners(listeners);\n      })\n      .on('broadcast', { event: 'pong' }, (payload: any) => {\n        const { listenerId, timestamp } = payload.payload;\n        const latency = Date.now() - timestamp;\n        setListenerPings(prev => ({ ...prev, [listenerId]: latency }));\n        \n        // Broadcast calculated ping back to listener so they can display it\n        channel.send({\n          type: 'broadcast',\n          event: 'ping_response',\n          payload: { listenerId, ping: latency }\n        });\n      })\n      .subscribe(async (status) => {\n        if (status === 'SUBSCRIBED') {\n          await channel.track({\n            role: 'host',\n            onlineAt: new Date().toISOString()\n          });\n        }\n      });\n\n    return () => {\n      channel.unsubscribe();\n      channelRef.current = null;\n    };\n  }, [isSessionActive, projectId]);\n\n  // Periodic 3s Pi
<truncated 4584 bytes>