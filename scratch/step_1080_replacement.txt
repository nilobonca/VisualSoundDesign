"    setActiveAreaIds(newActiveIds);\n    setProximityVolumes(newProximityVolumes);\n    setActiveAudioIds(newActiveAudioIds);\n    setSpatialPans(newSpatialPans);\n    setAudioFilters(newAudioFilters);\n\n    // Send broadcast updates for listeners\n    if (isSessionActive && channelRef.current && sessionListeners.length > 0) {\n      sessionListeners.forEach(listener => {\n        const pinId = `listener:${listener.listenerId}`;\n        const listenerPin = pins.find(p => p.id === pinId);\n        if (listenerPin) {\n          const hotspot = { x: listenerPin.position.x + 24, y: listenerPin.position.y + 48 };\n          const listenerAudios: any[] = [];\n\n          areas.forEach(area => {\n            if (isItemInPage(area.id) && area.linkedAudioId) {\n              if (isPointInPolygon(hotspot, area.points)) {\n                const audio = savedAudios.find(a => a.id === area.linkedAudioId);\n                if (audio) {\n                  let factor = 1;\n                  let pan = 0;\n                  if (area.volumeMode === 'proximity') {\n                    const sourcePoint = area.volumeSourcePoint || getPolygonCentroid(area.points);\n                    const dx = hotspot.x - sourcePoint.x;\n                    const dy = hotspot.y - sourcePoint.y;\n                    const distance = Math.sqrt(dx * dx + dy * dy);\n                    const radius = area.proximityRadius || 300;\n                    if (distance < radius) {\n                      factor = 1 - (distance / radius);\n                      pan = (sourcePoint.x - hotspot.x) / radius;\n                      pan = Math.max(-1, Math.min(1, pan));\n                    } else {\n                      factor = 0;\n                    }\n                  }\n\n                  if (factor > 0) {\n                    listenerAudios.push({\n                      audioId: audio.id,\n                      name: audio.name,\n                      url: audio.publicUrl || audio.url,\n                      volume: (area.volume ?? 1.0) * factor,\n       
<truncated 646 bytes>